version: '3.9'
services:
  app:
    build: .
    restart: always
    ports:
      - 8080:8080
    volumes:
      - .:/usr/src/app

  redis:
    image: redis:alpine
    restart: always
    ports:
      - 6379:6379
